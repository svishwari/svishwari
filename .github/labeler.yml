
# Add 'API' label to any change within the hux/api files within source dir
API:
- hux/api/*
- hux/api/**/*

# Add 'UX/UI' label to any change to files within the hux/frontend files within source dir
UX/UI:
- hux/frontend/*
- hux/frontend/**/*

# Add 'DB' label to any change to lib/ files within the source dir
DB:
- lib/*
- lib/**/*

# Add 'API_INT_TESTS' label to any change to lib/ files within the source dir
API_INT_TESTS:
- hux/integration_tests/api/*
- hux/integration_tests/api/**/*

# Add 'UI_INT_TESTS' label to any change to lib/ files within the source dir
UI_INT_TESTS:
- hux/integration_tests/frontend/*
- hux/integration_tests/frontend/**/*