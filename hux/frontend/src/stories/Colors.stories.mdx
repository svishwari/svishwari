import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs/blocks"
import { colors } from "./colors"
const renderColors = (colors) => {
  return colors.map((item, index) => (
    <ColorItem key={index}>{item.title}</ColorItem>
  ))
}

<Meta title="Design System/Colors" />

# Colors

Below is our basic color palette grouped by types of usage:

<div>
  {Object.keys(colors).map((color) => (
    <div key={color}>
      <h1 key={`head-${color}`}>{color}</h1>
      <div style={{ display: "flex", flexWrap: "wrap" }}>
        {Object.values(colors[color]).map((col) => (
          <div
            style={{
              backgroundColor: col.hex,
              border: "solid 1px #e2eaec",
              height: "180px",
              width: "250px",
              marginRight: "20px",
              marginBottom: "20px",
              borderRadius: "20px",
            }}
          >
            <div
              style={{
                display: "flex",
                flexDirection: "column",
                alignItems: "center",
                justifyContent: "center",
                height: "100%",
                color: "#FF4785",
              }}
            >
              <span>{col.title}</span>
              <span>CSS class:</span>
              <span>{`var(--v-${col.class}-base)`}</span>
              <span class="hover">{col.hex}</span>
            </div>
          </div>
        ))}
      </div>
    </div>
  ))}
</div>
