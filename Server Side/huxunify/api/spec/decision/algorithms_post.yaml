Invokes the algorithm
---
tags:
  - decisioning

parameters:
- name: algorithm_name
  in: path
  description: algorithm name
  required: true
  type: string
  default: dolong_deloitte_com:hux_unified_test
- name: body
  in: body
  description: algorithm inputs
  required: true
  example:
    algorithm_name: dolong_deloitte_com:hux_unified_test
    params: s3://xspdev-tecton-shared/advertiser-data-scoring-data-store/test_with_uid.parquet

responses:
  200:
    description:
      OK
    schema:
      $ref: "#/definitions/algorithms"
  404:
    description:
      failed to run the algorithm